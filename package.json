{
  "name": "ai-global-index-prediction-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build && rm -rf dist && cp -r frontend/dist dist",
    "dev": "npm --prefix frontend run dev"
  }
}
