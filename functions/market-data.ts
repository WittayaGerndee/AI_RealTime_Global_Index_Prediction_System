// Pages Functions are read from the project root. The implementation lives next to the
// frontend so the endpoint works whether the Pages root directory is the repo or `frontend`.
export { onRequestGet } from '../frontend/functions/market-data';
